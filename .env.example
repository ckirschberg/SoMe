# Copy to .env and fill in the values for your machine.
# .env is gitignored; this file is committed so everyone knows which names to set.
DB_HOST=localhost
DB_PORT=5432
DB_USER=
DB_PASS=
DB_NAME=some
